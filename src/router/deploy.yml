name: Build Vue

on: [push]

permissions:
  contents: write

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
      - uses: actions/checkout@v4
      - id: Build-Vue
        uses: xRealNeon/VuePagesAction@1.0.1
        with:
          username: 'hostingweb146-oss'
          reponame: 'system-student-frontend'
          token: ${{ secrets.GITHUB_TOKEN }}
